[project]
name = "rc-neuroevo"
version = "0.1.0"
description = "Code Bullet style neuroevolution cars in pygame"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "pygame",
  "tensorflow"
]

[tool.setuptools]
packages = ["neuroevo"]
